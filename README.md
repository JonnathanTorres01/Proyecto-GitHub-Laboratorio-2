git config user.name "JonnathanTorres01 "
git config --global user.email jonnathan.torres.m94@gmail.com
git add .
git branch desarrollo
git commit -m "agrega contenido en index"
git merge desarrollo
git commit -m "cambio temporal"
git status .  
git commit -m "Estructura inicial del proyecto"
git log --graph --decorate --all --oneline
git branch -m desarrollo
git commit -desarrollo "agrega contenido en index"
git commit -m "agrega contenido en index"
git branch desarrollo
git branch
git merge desarrollo
git commit -m "cambio temporal"
git reset --hard HEAD~1
git reflog
.gitignore
git commit -m "Agrega gitignore"
git init
git branch rama1
git branch -m main 
git remote add origin https://github.com/JonnathanTorres01/Proyecto-GitHub-Laboratorio-2.git
git add .
git commit -m "Agrega Login"
git push -u origin main

